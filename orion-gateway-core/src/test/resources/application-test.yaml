# Orion Gateway URI Sanitizing 配置

orion:
  gateway:
    uri-sanitizing:
      # 是否启用 URI 清理功能
      enabled: true
      # 需要处理的路径模式（支持通配符 * 和 **）
      path-patterns:
        - /api/**
        - /service/**
